{
  "name": "react-notifications",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.6.1",
    "react-addons-css-transition-group": "^15.6.0",
    "react-dom": "^15.6.1",
    "react-popover": "^0.4.16",
    "react-scripts": "0.9.5",
    "react-tabs": "^1.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "docs": "react-scripts build && mv build docs",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "git subtree push --prefix build/ origin gh-pages",
    "yarn": "yarn install --ignore-engines --no-lockfile",
    "build": "babel src/notifications.js --out-file index.js --presets es2015,react"
  },
  "homepage": ".",
  "main": "dist/index.js"
}
